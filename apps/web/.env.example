# Leave AGENT_ENDPOINT unset to run against the scripted mock.
# Set MOCK=1 to force the mock even when the endpoint is configured — this is the
# demo fallback if SAP or the venue network fails.
AGENT_ENDPOINT=
AGENT_TOKEN=

AWS_REGION=us-east-1
INVOICE_BUCKET=516359819848-invoice
