fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.9.0

# Live SAP + Bedrock. Not needed to run against the fake backends.
boto3>=1.35.0
# sap.py imports this directly. It used to arrive transitively via mcp, which
# meant removing an unused dependency would have broken live SAP.
requests>=2.32.0
python-dotenv>=1.0.0
